/** * Phase 12: Events & Seasons — localStorage Schema * Typed getters/setters for event data persistence */ import type { EventCalendarState } from './calendarModel'; import { getDefaultCalendarState, isEventCalendarState } from './calendarModel'; // Storage keys const STORAGE_KEY = 'city_builder_events'; const LAST_MIGRATION_KEY = 'city_builder_events_migration'; /** * Get event data from localStorage * Returns typed EventCalendarState or default if not found/invalid */ export const getEventData = (): EventCalendarState => { if (typeof window === 'undefined') { return getDefaultCalendarState(); } try { const raw = localStorage.getItem(STORAGE_KEY); if (!raw) { return getDefaultCalendarState(); } const parsed = JSON.parse(raw); if (!isEventCalendarState(parsed)) { console.warn('[Events] Invalid data structure in localStorage, resetting'); return getDefaultCalendarState(); } return parsed; } catch (err) { console.error('[Events] Failed to load event data:', err); return getDefaultCalendarState(); } }; /** * Save event data to localStorage */ export const setEventData = (data: EventCalendarState): boolean => { if (typeof window === 'undefined') { return false; } try { localStorage.setItem(STORAGE_KEY, JSON.stringify(data)); return true; } catch (err) { console.error('[Events] Failed to save event data:', err); return false; } }; /** * Get specific currency balance */ export const getCurrencyBalance = (currencyId: string): number => { const data = getEventData(); return data.currencies[currencyId] ?? 0; }; /** * Earn/spend event currency * Returns new balance or null on error */ export const modifyCurrency = ( currencyId: string, delta: number ): number | null => { const data = getEventData(); const current = data.currencies[currencyId] ?? 0; const newBalance = current + delta; if (newBalance < 0) { return null; // Insufficient funds } data.currencies[currencyId] = newBalance; data.lastViewedAt = new Date().toISOString(); const saved = setEventData(data); return saved ? newBalance : null; }; /** * Earn currency (wrapper for clarity) */ export const earnCurrency = (currencyId: string, amount: number): number | null => { if (amount <= 0) return null; return modifyCurrency(currencyId, amount); }; /** * Spend currency (wrapper with validation) */ export const spendCurrency = (currencyId: string, amount: number): number | null => { if (amount <= 0) return null; return modifyCurrency(currencyId, -amount); }; /** * Add completed event to history */ export const addCompletedEvent = (eventId: string): boolean => { const data = getEventData(); if (!data.completedEventIds.includes(eventId)) { data.completedEventIds.push(eventId); return setEventData(data); } return true; }; /** * Add completed season to history */ export const addCompletedSeason = (seasonId: string): boolean => { const data = getEventData(); if (!data.completedSeasonIds.includes(seasonId)) { data.completedSeasonIds.push(seasonId); return setEventData(data); } return true; }; /** * Check if an event is completed */ export const isEventCompleted = (eventId: string): boolean => { const data = getEventData(); return data.completedEventIds.includes(eventId); }; /** * Check if a season is completed */ export const isSeasonCompleted = (seasonId: string): boolean => { const data = getEventData(); return data.completedSeasonIds.includes(seasonId); }; /** * Update seasons in state */ export const updateSeasons = (seasons: Record<string, unknown>): boolean => { const data = getEventData(); data.seasons = seasons as EventCalendarState['seasons']; return setEventData(data); }; /** * Update events in state */ export const updateEvents = (events: Record<string, unknown>): boolean => { const data = getEventData(); data.events = events as EventCalendarState['events']; return setEventData(data); }; /** * Update community events in state */ export const updateCommunityEvents = (communityEvents: Record<string, unknown>): boolean => { const data = getEventData(); data.communityEvents = communityEvents as EventCalendarState['communityEvents']; return setEventData(data); }; /** * Set active season */ export const setActiveSeason = (seasonId: string | null): boolean => { const data = getEventData(); data.activeSeasonId = seasonId; return setEventData(data); }; /** * Get active season ID */ export const getActiveSeasonId = (): string | null => { const data = getEventData(); return data.activeSeasonId; }; /** * Check if event is currently active */ export const isEventActive = (eventId: string): boolean => { const data = getEventData(); const event = data.events[eventId]; if (!event) return false; return event.lifecycle === 'active'; }; /** * Check if season is currently active */ export const isSeasonActive = (seasonId: string): boolean => { const data = getEventData(); const season = data.seasons[seasonId]; if (!season) return false; return season.lifecycle === 'active'; }; /** * Get event by ID */ export const getEvent = (eventId: string) => { const data = getEventData(); return data.events[eventId] ?? null; }; /** * Get season by ID */ export const getSeason = (seasonId: string) => { const data = getEventData(); return data.seasons[seasonId] ?? null; }; /** * Get community event by ID */ export const getCommunityEvent = (communityEventId: string) => { const data = getEventData(); return data.communityEvents[communityEventId] ?? null; }; /** * Update notification preferences */ export const setNotificationsEnabled = (enabled: boolean): boolean => { const data = getEventData(); data.notificationsEnabled = enabled; return setEventData(data); }; /** * Dismiss a notification */ export const dismissNotification = (notificationId: string): boolean => { const data = getEventData(); if (!data.dismissedNotifications.includes(notificationId)) { data.dismissedNotifications.push(notificationId); return setEventData(data); } return true; }; /** * Check if notification is dismissed */ export const isNotificationDismissed = (notificationId: string): boolean => { const data = getEventData(); return data.dismissedNotifications.includes(notificationId); }; /** * Clear all event data (for testing/reset) */ export const clearEventData = (): boolean => { if (typeof window === 'undefined') return false; try { localStorage.removeItem(STORAGE_KEY); localStorage.removeItem(LAST_MIGRATION_KEY); return true; } catch (err) { console.error('[Events] Failed to clear event data:', err); return false; } };
