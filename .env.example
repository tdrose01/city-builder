# Copy to .env and fill in values (used by scripts\Invoke-DbMigrations.ps1 and Node API)
PGHOST=localhost
PGPORT=5432
PGDATABASE=city-builder_dev
PGUSER=postgres
PGPASSWORD=CHANGE_ME

# Optional: Node API
NODE_API_PORT=5001

# Optional: .NET API
DOTNET_URLS=http://localhost:5002